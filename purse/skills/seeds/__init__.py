"""Bundled seed skill artifacts (C5.4).

The ``*.md`` files here are real skill documents preloaded into every new vault by
:func:`purse.skills.seed.seed_default_skills`. This is a package (not a bare
directory) so :func:`importlib.resources.files` can enumerate the artifacts from
an installed wheel as well as from the source tree.
"""
